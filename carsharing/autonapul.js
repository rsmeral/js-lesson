/**
 * Calculates price per trip by Autonapůl.
 * 
 * @param {number} distance Distance travelled in kilometers
 * @param {number} duration Duration of trip in minutes
 * @returns {number} Price in Kč
 */
const calculateTripPrice = (distance, duration) => {
  // TODO: Write your code here.
  // The contract of this function must stay - name, parameters, return type.
  // You can also write and use any other functions, variables, and modules.
}

module.exports = {
  calculateTripPrice
};
